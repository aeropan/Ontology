# data_explorer 操作手册

## 目录结构

```
项目文件夹/
├── data_explorer.html     # 主页面
├── update_data.py         # 数据更新脚本
└── data/
    ├── data_explorer.xlsx # Excel 数据源
    ├── explorer_data.json # 自动生成，勿手动修改
    └── method_config.json
```

---

## 一、首次环境配置（只需做一次）

**1. 确认 Python 已安装**
在 Cursor 终端输入：
```
python --version
```
显示 `Python 3.x.x` 即可。没有则去 python.org 下载安装。

**2. 安装依赖**
```
pip install openpyxl
```

---

## 二、日常更新数据

**步骤：**
1. 把新 Excel 替换 `data/data_explorer.xlsx`
2. 在 Cursor 中右键点击 `update_data.py` → **Run Python File**
3. 看到"完成！"提示即成功
4. 重新打开 `data_explorer.html` 查看最新数据

---

## 三、临时预览（不替换文件）

打开页面 → 点击右上角**「刷新数据」**按钮 → 选择 Excel 文件
> 仅当次有效，关闭页面后恢复原数据

---

## 四、常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| 运行脚本报错"Excel 不存在" | Excel 路径不对 | 确认文件在 `data/` 文件夹内且名称为 `data_explorer.xlsx` |
| 数据没有更新 | 忘记重新打开 HTML | 关掉浏览器标签页重新打开 |
| pip 命令找不到 | Python 未正确安装 | 重新安装 Python，勾选"Add to PATH" |
