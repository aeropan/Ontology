# RAG 增强管理模块 · 产品需求文档（PRD）

---

## 模块概述

RAG 增强管理（Retrieval-Augmented Generation 增强管理）是本体智见平台的核心服务模块之一，用于配置和管理检索增强生成规则集。用户可通过该模块将私有知识文档与大模型推理能力结合，为特定业务场景（如风电预测、气象灾害预警等）定制化配置知识检索策略、文档切片参数与生成指令，从而提升大模型在垂直领域的问答质量与可信度。

**入口路径**：左侧导航栏 → 服务中心 → RAG增强管理

---

## 一、RAG增强管理 · 规则列表页

**路由文件**：`RAGrule_list.html`

### 【页面结构】

- **页头区域**：
  - 页面标题：「RAG增强管理」；
  - 副标题说明：「管理检索增强生成（RAG）规则集，配置知识检索与增强策略」；
  - 右上角主操作按钮：「＋ 新建RAG规则」，点击跳转至规则集配置页（新建模式）；

- **规则卡片列表**（卡片网格布局，列数随屏宽自适应，最小卡片宽度300px）：
  - 每张卡片代表一个 RAG 规则集，包含以下内容：
    - **顶部颜色条**：通过 `data-color` 属性驱动（青色 `cyan` / 琥珀色 `amber` / 紫色 `purple`），用于区分规则集分类或重要程度；
    - **规则集名称**；
    - **描述说明**：最多展示两行，超出部分以省略号截断；
    - **关联本体徽章**：展示该规则集绑定的本体名称，使用与顶部颜色条匹配的彩色标签；若无关联本体则展示灰色「无关联本体」；
    - **创建时间**；
    - **操作菜单**（卡片右上角三点图标按钮，点击展开下拉菜单，点击卡片其他区域或页面空白处关闭）；

- **删除确认弹窗**：
  - 触发条件：点击操作菜单中的「删除」；
  - 弹窗内展示目标规则集名称，明确提示「操作不可撤销，相关配置将永久移除」；
  - 操作按钮：「取消」（关闭弹窗）/ 「确认删除」（执行删除）；
  - 点击弹窗遮罩层可关闭弹窗；

- **Toast 轻提示**：操作完成（复制/删除）后，右下角浮现短暂文字提示，2.5秒后自动消失；

### 【规则卡片操作说明】

| 操作 | 触发方式 | 跳转逻辑 | 说明 |
|------|---------|---------|------|
| 查看 / 编辑 | 点击卡片主体 或 操作菜单 → 「修改」 | 跳转至【规则集配置页】 | 进入已有规则集的详情与编辑视图 |
| 复制 | 操作菜单 → 「复制」 | 无跳转，Toast 提示 | 在当前列表末尾创建同名副本 |
| 删除 | 操作菜单 → 「删除」 | 弹出删除确认弹窗 | 二次确认后从列表移除，操作不可撤销 |
| 新建 | 页头「＋ 新建RAG规则」按钮 | 跳转至【规则集配置页】（新建模式） | — |

### 【数据接口】

- **规则集列表接口**：返回所有 RAG 规则集，字段包括：规则集 ID / 名称 / 描述 / 关联本体（名称 + 颜色标识）/ 创建时间；
- **删除规则集接口**：传入规则集 ID，执行物理删除；
- **复制规则集接口**：传入规则集 ID，服务端克隆配置后返回新副本数据（含新 ID）；

---

## 二、RAG增强管理 · 规则集配置页

**路由文件**：`RAGrule_detail.html`

### 【页面结构】

**顶部导航栏**：
- 面包屑导航：服务中心 › RAG增强管理 › 新建规则集；
- 右侧操作按钮：「返回」（不保存，返回列表页）/ 「确认添加」（校验并保存，返回列表页）；

---

**表单配置区**（页面左侧主体，内容可滚动，支持长表单）：

**基本信息**

- 规则集名称（文本输入框，必填，placeholder 举例：`2026风电场网络协议合规检查`）；

---

**知识源选择**

- 操作按钮行（两个并排按钮）：
  - 「上传文档」（蓝色实心按钮）：点击唤起本地文件选择框，支持格式 `.pdf / .xlsx / .docx / .txt`，支持多文件同时选择；上传后文件自动追加至下方文献列表顶部；
  - 「从文献库选择」（描边样式按钮）：从平台已有文献库中选取文献（功能预留，点击后弹出选择弹窗）；

- **已选文献列表表格**：展示当前规则集已绑定的文献；
  - 字段：序号 / 文献名称 / 格式 / 大小 / 上传时间 / 状态 / 操作；
  - 格式徽章：PDF（红色）/ XLSX（绿色）/ DOCX（蓝色）/ TXT（灰色）；
  - 状态列：固定展示「已解析」（绿色圆点 + 文字）；
  - 操作列：「移除」按钮，点击后该条记录从列表中删除；
  - 最多展示 **10行**，超出10条时列表底部出现分页控件（显示总篇数与当前页码，提供上一页/下一页按钮）；
  - 若列表为空则展示空状态提示「暂无文献，请上传或从文献库选择」；

---

**Section ❶ 解析与切片策略**

| 字段 | 控件类型 | 可选值 / 范围 | 默认值 | 说明 |
|------|---------|------------|-------|------|
| 解析方式 | 下拉选择 | Layout-Aware（支持图表与图像识别）/ Plain Text（纯文本快速提取）/ OCR Enhanced（扫描件/图片增强） | Layout-Aware | 影响文档内容的抽取方式 |
| 切片大小（Chunk Size） | 滑块 | 100 ~ 2000，步长50，单位 T | 500 T | 每个检索片段包含的 token 数量上限 |
| 重叠度（Overlap） | 滑块 | 0 ~ 500，步长10，单位 T | 50 T | 相邻切片之间共享的 token 数量，防止边界信息丢失 |

---

**Section ❷ 检索增强策略**

| 字段 | 控件类型 | 可选值 / 范围 | 默认值 | 说明 |
|------|---------|------------|-------|------|
| 语义检索权重（Hybrid Search） | 滑块 | 0 ~ 1，步长0.05 | 0.7 | 向右偏移提升专有名词的精确关键词匹配能力；向左偏移语义理解优先 |
| 检索深度（Top-K） | 数字输入框 | 1 ~ 20 | 5 | 单次检索召回并返回给模型的最相关片段数量 |

---

**Section ❸ 重排与质量控制**

| 字段 | 控件类型 | 可选值 / 范围 | 默认值 | 说明 |
|------|---------|------------|-------|------|
| 开启 Rerank 二次重排 | 复选框 | 开启 / 关闭 | 开启 | 启用后对 Top-K 结果进行语义重排序，显著提升过滤准确率 |
| 置信度阈值（Confidence Score） | 滑块 | 0 ~ 1，步长0.01 | 0.75 | 低于此分数的片段将被过滤，不传入模型；建议保持 0.6 ~ 0.8 区间 |

---

**Section ❹ 业务逻辑生成指令（Prompt）**

- System Prompt 指令（多行文本框）：定义大模型在生成回答时的角色、格式要求与输出规范；
- placeholder 示例：「你是一位电力系统运维专家。请根据检索到的参考文档，若请求中出现"参考/基于/在"的内容，请将格式转化为JSON格式，必须包含 `source_page` 字段。」

---

**右侧实时效果仿真抽屉**（宽度 310px，从页面右侧滑入）：

- **收纳/展开 Tab**：固定悬浮于视口右侧，点击切换抽屉展开/收纳状态，Tab 位置跟随抽屉动画平移；
- **抽屉内容**：
  - 测试问题输入框（placeholder：`例：当风速超过14m/s时如何处理？`）；
  - 「开始模拟效果」按钮；
  - 加载状态（检索中...）；
  - 检索结果展示区：
    - 检索回传片段列表（含文档来源、页码、相似度分数）；
    - 源端原始结果 JSON 代码块；

### 【抽屉交互状态说明】

| 抽屉状态 | Tab 箭头方向 | Tab 视觉位置 | 说明 |
|---------|------------|------------|------|
| 收纳（默认） | ← 向左 | 固定在视口最右侧 | 点击后抽屉从右侧滑入展开 |
| 展开 | → 向右 | 距视口右边缘 310px | 点击后抽屉滑出收纳，Tab 同步回右侧 |

### 【实时仿真状态说明】

| 步骤 | 状态 | UI 表现 |
|-----|------|--------|
| 点击「开始模拟效果」 | 加载中 | 按钮进入 loading 态（禁用 + 旋转动画），隐藏旧结果 |
| 检索完成（约1.3s后） | 结果展示 | 按钮恢复可用，展示检索片段列表与原始 JSON 结果块 |

### 【数据接口】

- **规则集详情接口**：编辑模式下根据规则集 ID 返回完整配置，字段包括：名称 / 知识源列表 / 解析方式 / 切片大小 / 重叠度 / 语义权重 / Top-K / Rerank开关 / 置信度阈值 / System Prompt；
- **新建/保存规则集接口**：提交以上全部配置字段，服务端写入后返回规则集 ID；
- **知识源文献列表接口**：返回当前规则集绑定的文献，字段：文献 ID / 名称 / 格式 / 大小 / 上传时间 / 解析状态；
- **文献上传接口**：接收 `multipart/form-data`，完成解析后返回文献元数据（ID / 名称 / 格式 / 大小 / 解析状态）；
- **移除知识源接口**：传入文献 ID，解除与当前规则集的绑定关系；
- **实时仿真接口**：传入测试问题文本及当前策略参数（Top-K / 语义权重 / 置信度阈值），返回检索片段列表及原始向量检索结果 JSON；

---

## 三、页面跳转关系总览

| 来源 | 触发操作 | 目标页 | 备注 |
|------|---------|-------|------|
| 左侧导航栏 | 点击「RAG增强管理」 | 规则列表页 | — |
| 规则列表页 | 点击「＋ 新建RAG规则」 | 规则集配置页（新建模式） | — |
| 规则列表页 | 点击卡片主体 或 操作菜单→「修改」 | 规则集配置页（编辑模式） | 携带规则集 ID |
| 规则集配置页 | 点击「返回」 | 规则列表页 | 不保存当前配置 |
| 规则集配置页 | 点击「确认添加」 | 规则列表页 | 保存成功后返回列表，新规则集出现在列表中 |

---

*文档版本：v1.0 · 最后更新：2026-04-12*
