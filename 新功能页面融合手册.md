# 新功能页面融合操作手册

---

## 一、让大模型设计页面时的提示词规范

每次让大模型（Claude 或其他）设计新页面时，在提示词里必须包含以下说明：

---

### 复制粘贴这段话给大模型：

```
请帮我设计一个 HTML 页面，需要遵守以下规范：

【样式规范】
使用以下 CSS 变量，不要自定义颜色值：
  --bg: #080c18
  --sidebar-bg: #0d1220
  --surface: #0f1729
  --surface2: #131d30
  --surface3: #1a2540
  --border: #1e2d4a
  --border2: #253550
  --blue: #3b82f6
  --blue-dim: rgba(59,130,246,0.15)
  --cyan: #06b6d4
  --cyan-dim: rgba(6,182,212,0.12)
  --purple: #8b5cf6
  --green: #10b981
  --green-dim: rgba(16,185,129,0.12)
  --amber: #f59e0b
  --rose: #f43f5e
  --text1: #e2e8f0
  --text2: #94a3b8
  --text3: #475569

字体：'Noto Sans SC'（中文）和 'JetBrains Mono'（代码/数字），
从 Google Fonts 引入。

【结构规范】
- 不要包含左侧导航栏
- 不要包含顶部标题栏
- body 样式设为：
  background: var(--bg);
  font-family: 'Noto Sans SC', sans-serif;
  color: var(--text1);
  min-height: 100vh;
  overflow-y: auto;
  margin: 0;
  padding: 24px;
  box-sizing: border-box;

【跳转规范】
- 页面内不同视图之间的切换，用自己的 JS 函数控制显示/隐藏
- 不要使用 window.location.href 跳转到其他页面
- 如果需要弹窗，用 CSS class 控制显示（display:none / display:flex），
  不要用 alert() 或 confirm()
```

---

## 二、拿到 HTML 文件后的融合步骤

假设大模型给你生成了一个新文件，比如 `new_feature.html`。

### 第一步：检查新文件，确认三件事

打开新文件，确认：

1. **有没有导航栏**：搜索 `<nav` 或 `sidebar`，如果有，需要删掉
2. **有没有顶部栏**：搜索 `topbar` 或 `<header`，如果有，需要删掉
3. **body 样式是否正确**：搜索 `body {`，确认有 `overflow-y: auto` 而不是 `overflow: hidden`

如果大模型按照上面的提示词规范来做，这三件事通常不需要额外处理。

---

### 第二步：修改新文件的 body 样式

找到新文件 `<style>` 里的 `body` 规则，确保它是这样的：

```css
body {
  background: var(--bg);
  font-family: 'Noto Sans SC', sans-serif;
  color: var(--text1);
  min-height: 100vh;
  overflow-y: auto;
  margin: 0;
  padding: 24px;
  box-sizing: border-box;
}
```

如果有其他多余的 `body {}` 规则（比如 `body { padding: 10px; }` 之类），全部删掉，只保留上面这一个。

---

### 第三步：修改 shell.html（index.html），加入导航入口

用 VS Code 打开 `shell.html`，需要改两处：

**改动 A：在侧边栏加导航项**

在侧边栏找到合适的位置（比如"服务中心"区域下），加一个导航项：

```html
<div class="nav-item" id="nav-新模块英文名" onclick="navigate('new_feature.html', null, ['新模块名称'])">
  <svg class="nav-icon" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5">
    <!-- 选一个合适的图标 SVG，参考下面的图标库 -->
    <rect x="1" y="10" width="4" height="5" rx="0.5"/>
    <rect x="6" y="6" width="4" height="9" rx="0.5"/>
    <rect x="11" y="2" width="4" height="13" rx="0.5"/>
  </svg>
  新模块名称
</div>
```

注意三个需要替换的地方：
- `nav-新模块英文名`：自己起一个英文 id，比如 `nav-report`
- `new_feature.html`：新文件的实际文件名
- `新模块名称`：显示在导航栏上的中文名

**改动 B：在 NAV_MAP 里注册路由**

在 `shell.html` 的 `<script>` 里找到 `const NAV_MAP = {` 这段，在最后一条记录后面加一行：

```javascript
'nav-新模块英文名': { file: 'new_feature.html', page: null, crumbs: ['新模块名称'] },
```

例如：

```javascript
const NAV_MAP = {
  'nav-home':     { file: 'main.html', page: 'home',    crumbs: ['概览', '主页'] },
  'nav-industry': { file: 'data_explorer.html', page: null, crumbs: ['行业分析'] },
  // 新加这一行 ↓
  'nav-report':   { file: 'new_feature.html', page: null, crumbs: ['新模块名称'] },
};
```

---

### 第四步：把新文件放到正确位置

把 `new_feature.html` 和 `shell.html`、`main.html`、`data_explorer.html` 放在**同一个文件夹**里。

---

### 第五步：测试

双击打开 `shell.html`（或 `index.html`），点击新加的导航项，确认：
- 右侧内容区正常显示新页面内容
- 上下左右有正常的间距
- 页面内部交互（弹窗、切换等）正常工作

---

## 三、图标参考

shell.html 侧边栏的导航图标都是 SVG，下面提供几个常用的，复制 `<svg>...</svg>` 整段替换即可：

**图表/分析类**
```html
<svg class="nav-icon" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5">
  <rect x="1" y="10" width="4" height="5" rx="0.5"/><rect x="6" y="6" width="4" height="9" rx="0.5"/><rect x="11" y="2" width="4" height="13" rx="0.5"/>
</svg>
```

**文档/报告类**
```html
<svg class="nav-icon" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5">
  <path d="M4 2h6l3 3v9a1 1 0 01-1 1H4a1 1 0 01-1-1V3a1 1 0 011-1z"/><path d="M10 2v3h3M6 7h5M6 10h5M6 13h3"/>
</svg>
```

**设置/配置类**
```html
<svg class="nav-icon" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5">
  <circle cx="8" cy="8" r="2"/><path d="M8 1v2M8 13v2M1 8h2M13 8h2M3.05 3.05l1.41 1.41M11.54 11.54l1.41 1.41M3.05 12.95l1.41-1.41M11.54 4.46l1.41-1.41"/>
</svg>
```

**用户/团队类**
```html
<svg class="nav-icon" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5">
  <circle cx="6" cy="5" r="3"/><path d="M1 14s1-4 5-4 5 4 5 4"/><circle cx="12" cy="5" r="2"/><path d="M15 14s-.5-3-3-3"/>
</svg>
```

**数据库类**
```html
<svg class="nav-icon" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5">
  <ellipse cx="8" cy="4" rx="6" ry="2"/><path d="M2 4v4c0 1.1 2.69 2 6 2s6-.9 6-2V4"/><path d="M2 8v4c0 1.1 2.69 2 6 2s6-.9 6-2V8"/>
</svg>
```

---

## 四、注意事项

**关于文件命名**：新文件名只用英文小写字母、数字和下划线，比如 `data_report.html`、`user_mgmt.html`，不要用中文或空格。

**关于弹窗**：新页面如果有弹窗，确保弹窗的 `z-index` 不要超过 `1000`，否则可能穿透 iframe 边界显示异常。实际上 iframe 内的弹窗不会真的穿透，但视觉上会被裁切，所以弹窗的父容器要是 `position: fixed` 且在 body 下。

**关于页面内跳转**：如果新页面内部有多个视图（比如列表页和详情页），用 JS 控制 `display: none / block` 切换即可，不需要通知 shell.html，shell.html 不关心 iframe 内部的视图状态。

**关于面包屑同步**：如果新页面内部发生视图切换，想让 shell.html 顶部的面包屑也同步更新，可以在新页面的 JS 里加：

```javascript
// 在视图切换函数里调用这个，更新外层面包屑
function notifyShell(crumbs) {
  if (window.parent !== window) {
    window.parent.postMessage({ type: 'NAV', crumbs: crumbs }, '*');
  }
}

// 使用示例：切换到详情视图时
function showDetail() {
  document.getElementById('list-view').style.display = 'none';
  document.getElementById('detail-view').style.display = 'block';
  notifyShell(['行业分析', '风力发电功率预测']);  // 面包屑更新
}
```
